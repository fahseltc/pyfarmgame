class TileType:
    NORMAL = "NORMAL"
    COLLISION = "COLLISION"


    # maybe use constants here to represent game objects? EX Grass, stone, wall, door, etc. each tile gets a type?
    # then later the renderer just look at the tile types and draws